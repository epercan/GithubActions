name: Log action
'on':
  label:
    types:
      - created
  issues:
    types:
      - opened
jobs:
  log:
    runs-on: ubuntu-latest
    steps:
        echo "Trigger - ${{ github.event_name }},user - ${{ github.actor }},
        date - `date`"
